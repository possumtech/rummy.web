# Unit-test fixture. Mirrors the values rummy/main's .env.example
# declares — kept here so `node --test` can run rummy.web in
# isolation without depending on the parent app's cascade.
RUMMY_WEB_SEARCH_MAX=1
RUMMY_WEB_SEARCH_RESULTS=20
RUMMY_TOKEN_DIVISOR=2
RUMMY_WEB_FETCH_TIMEOUT=300000
